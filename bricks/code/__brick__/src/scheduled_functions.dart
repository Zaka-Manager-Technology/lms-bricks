import 'types.dart';

/// A scheduled function is a function that is scheduled to run at a specific time.

List<Action> updateLoanInterestRate({ required Loan loan, required List<Borrower> borrowers }) {
  // Custom logic
  return []; // Optional list of actions to queue
}
